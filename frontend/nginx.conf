# Nginx : SPA React + reverse proxy vers Daphne (API, WebSockets, admin, santé)
server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    client_max_body_size 30m;

    # API REST
    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
    }

    # WebSockets (Channels)
    location /ws/ {
        proxy_pass http://backend:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    # Administration Django
    location /admin/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Sonde de santé
    location /health/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
    }

    # Fichiers statiques collectés par Django (admin)
    location /static/ {
        alias /srv/static/;
    }

    # Médias téléversés (volume partagé avec le backend)
    location /media/ {
        alias /srv/media/;
    }

    # SPA : toute route inconnue sert index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
